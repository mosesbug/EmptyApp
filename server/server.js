Recordings.allow({
  'insert': function () {
    // add custom authentication code here
    return true;
  }

});

// Template.displayHelper( 'displayHelper', () => {
//   return string.toUpperCase();
// });
